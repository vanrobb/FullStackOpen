```mermaid
    sequenceDiagram
    participant browser
    participant server

    browser->>server: POST /new_note_spa
    activate server
    server-->>browser: the notes w/new note from post
    deactivate server
    Note right of browser: the note is formatted into json and pushed to the server; in doing so it rerenders the note list
```